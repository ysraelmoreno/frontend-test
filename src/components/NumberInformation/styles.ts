import { styled } from "../../styles/Theme.provider";

export const NumberInformationData = styled("div", {
  display: "flex",
  flexDirection: "column",
  marginTop: "10px",

  p: {
    marginTop: "5px",
  },
});
